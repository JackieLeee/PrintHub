# Electron postinstall downloads platform binaries; do not skip on cache restore.
side-effects-cache=false

# Hoist sharp platform packages so electron-builder can bundle arm64 + x64 natives.
public-hoist-pattern[]=@img/*
public-hoist-pattern[]=sharp
